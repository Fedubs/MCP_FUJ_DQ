// Helper: Excel File Operations
import ExcelJS from 'exceljs';

// TODO: Move Excel reading/writing functions here
// - readExcelFile(filePath)
// - writeExcelFile(filePath, columnName, data)
// - detectColumnType(values)
// - profileColumn(columnName, values)

export { readExcelFile, writeExcelFile, detectColumnType, profileColumn };
